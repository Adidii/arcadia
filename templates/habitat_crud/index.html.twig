{% extends 'base.html.twig' %}

{% block title %}Habitat index{% endblock %}

{% block body %}
    <h1>Habitat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_habitat</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Image_url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for habitat in habitats %}
            <tr>
                <td>{{ habitat.idHabitat }}</td>
                <td>{{ habitat.nom }}</td>
                <td>{{ habitat.description }}</td>
                <td>{{ habitat.imageUrl }}</td>
                <td>
                    <a href="{{ path('app_habitat_crud_show', {'id_habitat': habitat.id_habitat}) }}">show</a>
                    <a href="{{ path('app_habitat_crud_edit', {'id_habitat': habitat.id_habitat}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_habitat_crud_new') }}">Create new</a>
{% endblock %}
