<form method="post" action="{{ path('app_habitat_crud_delete', {'id_habitat': habitat.id_habitat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habitat.id_habitat) }}">
    <button class="btn">Delete</button>
</form>
