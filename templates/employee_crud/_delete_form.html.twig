<form method="post" action="{{ path('app_employee_crud_delete', {'id_employe': employee.id_employe}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.id_employe) }}">
    <button class="btn">Delete</button>
</form>
