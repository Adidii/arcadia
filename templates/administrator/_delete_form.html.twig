<form method="post" action="{{ path('app_administrator_delete', {'id_admin': administrator.id_admin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrator.id_admin) }}">
    <button class="btn">Delete</button>
</form>
